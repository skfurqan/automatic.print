# Copy this file to .env on the SHOP PC (the one with the printer).
# This file must never be committed to git or shared — it's gitignored.

SUPABASE_URL=https://hrnwaltyfpljhrivzfbn.supabase.co

# Paste your real service_role secret key here (starts with sb_secret_...).
# Get it from: Supabase Dashboard > Project Settings > API > service_role
# This key bypasses all security rules — it belongs ONLY on the shop PC,
# never in the frontend, never in git.
SUPABASE_SERVICE_ROLE_KEY=

SHOP_SLUG=test-kiosk
POLL_SECONDS=3
